## Documentation updates

While implementing this plan, scan `doc/src/` for documentation that the changes have made inaccurate, and update it inline as part of the implementation.

In your final summary, list each file under `doc/src/` you updated, or state explicitly that nothing in `doc/src/` needed updating. Do not pad the report with speculative items.
