# browser-use subagent preamble (psibase)

Read `.cursor/skills/psibase-local-browser/SKILL.md` before navigating for useful info.

## Default origin

- `http://psibase.localhost:8080/` unless the parent task says otherwise.
- Verify psinode is up by reading `/check-psinode` output if a navigation
  fails.
